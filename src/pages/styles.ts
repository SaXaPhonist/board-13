import { styled } from '@mui/material/styles';

export const DashboardWrapper = styled('section')({
  gridColumn: '2 / -1',
  gridRow: '2',
  display: 'flex',
  flexDirection: 'row',
  gap: '3em',
});
